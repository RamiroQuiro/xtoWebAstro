---
import Links from "../../components/Links";
---

<nav class="w-full py-5 fixed top-0 text-gray-700 z-50 flex items-center justify-between px-5 bg-white/60 border-b  border-cyan-300/80">
    <div class="w-3/12 mx-auto flex bg-red-<500>">logo</div>
    <div class="w-6/12">
        <ul class="flex items-center justify-evenly">
            {
                Links?.map((link)=>(
    <li class="cursor-pointer">
        <a href={link.href}>
        {link.name}</a></li>

))
            }
        </ul>
    </div>
    <div class="w-3/12 mx-auto flex bg-red-<500>">otra cosa</div>
</nav>